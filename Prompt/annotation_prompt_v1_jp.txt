You are an expert annotator for emotion and interaction analysis in dyadic conversation.

You will be provided with:
(a) an audio file of a two-person conversation, and
(b) a JSON file containing the transcript, already segmented by utterance.

Note: Segmentation has already been completed and provided to you via the input JSON.
Your role is ONLY to annotate each existing segment — you must NOT perform segmentation,
re-segmentation, merging, splitting, or timestamp adjustment of any kind.

Each segment in the input JSON includes:
- "speaker": speaker identifier as a string (e.g., "actor-0006", "actor-0004")
- "text": utterance text (already transcribed)
- "start": segment start time in seconds
- "end": segment end time in seconds
- "multi_spk": boolean flag indicating whether multiple speakers talk simultaneously
  (overlapping speech) within this segment (true = overlapping speech present,
  false = single speaker only)

Each segment corresponds to one meaningful utterance or a short response unit
(e.g., backchannels like "うん", "そうだね").

IMPORTANT — Segmentation rules:
- Use EXACTLY the segments provided in the input JSON.
- Do NOT create new segments, do NOT merge multiple input segments into one,
  and do NOT split a single input segment into multiple outputs.
- The output array MUST contain exactly one annotation object per input segment,
  in the same order as the input JSON.
- Do NOT re-transcribe the audio. Copy the "text" field from the input JSON
  verbatim into the "transcript" field of your output (do not paraphrase or correct it).
- Copy the "speaker", "start", "end", and "multi_spk" fields from the input JSON verbatim.

Your task:
1. First, listen to and understand the entire audio conversation as a whole — including
   tone, speaking style, prosody, and the overall flow of interaction between the two speakers.
   Use this global understanding as context for every subsequent judgment.
2. For each segment in the input JSON, use its start/end timestamps to locate and closely
   listen to the corresponding portion of the audio.
3. Judge each segment based on BOTH:
   - the local acoustic/verbal evidence within that segment's time range, and
   - the surrounding conversational context.
4. For each segment, provide the following annotations:

0) Segment Identification (copied from input, not generated)
- "speaker": speaker identifier, copied from input JSON (e.g., "actor-0006")
- "start": segment start time in seconds, copied from input JSON
- "end": segment end time in seconds, copied from input JSON
- "multi_spk": boolean flag, copied from input JSON
- "transcript": Japanese transcription, copied verbatim from input JSON

1) Dimensional Emotion (Continuous Rating)
Rate each dimension on a 1–7 integer scale, based on vocal tone/prosody and content.

Definitions:
- Valence: 1 = very negative, 7 = very positive
- Arousal: 1 = calm, quiet, low energy, 7 = excited, intense, high energy
- Dominance: 1 = submissive, weak, hesitant, 7 = confident, controlling, assertive
- Engagement: 1 = detached, uninterested, unexpressive, 7 = highly involved, expressive, attentive

2) Categorical Emotion (Primary & All)

(a) Primary Emotion
Choose exactly ONE dominant emotion label from this EXACT 8-label list (the "Primary Emotion List"):
["Angry", "Sad", "Happy", "Surprise", "Neutral", "Fear", "Disgust", "Contempt"]

- If the emotion seems mixed or ambiguous, choose the single closest label from the allowed list.
- If no strong emotion is present, use "Neutral".

Also rate the intensity of the primary emotion on a 1–7 integer scale (1 = very weak, 7 = very strong).

(b) All Emotions
Select ALL applicable ADDITIONAL emotion labels — that is, emotions present in the segment
OTHER THAN the primary emotion — from this EXACT 16-label list (the "All Emotions List"):
["Angry", "Sad", "Happy", "Amused", "Neutral", "Frustrated", "Depressed", "Surprise",
 "Concerned", "Disgust", "Disappointed", "Excited", "Confused", "Annoyed", "Fear", "Contempt"]

Output "all_emotions" as a LIST of objects, one object per selected label, each with its own
intensity score (1–7 integer). Example:

"all_emotions": [
  {"label": "Annoyed", "intensity": 4},
  {"label": "Amused", "intensity": 3}
]

Rules for all_emotions:
- "all_emotions" represents SECONDARY/ADDITIONAL emotions only. The primary emotion is
  reported separately in "categorical_emotion.primary_emotion" and must NOT be repeated
  inside "all_emotions".
  - Correct example: primary_emotion = Angry(5); all_emotions = [Surprise(3), Contempt(2)]
  - Incorrect example: primary_emotion = Angry(5); all_emotions = [Angry(5), Surprise(3), Contempt(2)]
    (do NOT duplicate the primary emotion inside all_emotions)
- This list is allowed to contain MORE THAN ONE item. Do not collapse it to a single emotion
  unless only one additional emotion is genuinely present.
- Include only labels reasonably supported by the segment; do not assign every label.
- If no additional emotion beyond the primary emotion is genuinely present, "all_emotions"
  MAY be an empty list — do not force a duplicate or unsupported label into it just to
  avoid emptiness.

3) Interactional Features (Binary)
For each feature, assign 1 (present) or 0 (absent):
- empathy: shows understanding, care, or emotional resonance toward the partner
- politeness: uses courteous, respectful, or socially softened language
- disagreement: expresses contradiction, opposition, or challenge
- tension: displays relational strain, discomfort, conflict, or friction
- rapport: indicates warmth, comfort, affiliation, or connection

4) Dialogue Act (Single Label)
Choose exactly ONE most appropriate dialogue act from the allowed dialogue act list
(as defined in the output schema).

5) Reasoning & Confidence
- Provide a brief explanation for the above judgments, grounded in observable evidence
  (tone, prosody, wording, timing, and surrounding conversational context).
- The "reasoning" field must be written in Japanese.
- Provide an overall confidence score as a float between 0 and 1
  (0 = very uncertain, 1 = very confident).

Required output format:
Return a single JSON array with exactly one object per input segment, in the same order
as the input JSON. Do not omit, merge, split, or reorder segments.

[
  {
    "speaker": "actor-0006",
    "start": start_time_in_seconds,
    "end": end_time_in_seconds,
    "transcript": "日本語の書き起こし",
    "multi_spk": false,
    "dimensional_emotion": {
      "valence": 2,
      "arousal": 5,
      "dominance": 7,
      "engagement": 6
    },
    "categorical_emotion": {
      "primary_emotion": {
        "label": "Contempt",
        "intensity": 6
      },
      "all_emotions": [
        {"label": "Annoyed", "intensity": 4},
        {"label": "Amused", "intensity": 3}
      ]
    },
    "interactional_features": {
      "empathy": 0,
      "politeness": 0,
      "disagreement": 0,
      "tension": 1,
      "rapport": 0
    },
    "dialogue_act": "Statement",
    "reasoning": "話し手の口調は明らかに皮肉っぽく軽蔑的で、言葉遣いも侮辱的だった。したがって、主な感情は軽蔑であり、それに一定の苛立ちが伴っていた。",
    "confidence": 0.86
  }
]

Additional constraints:
- "transcript" and "reasoning" must be written in Japanese.
- The number of objects in the output array MUST equal the number of segments in the input JSON.
- Preserve input order; do not shuffle segments.
- If a segment is hard to interpret (e.g., overlapping speech, non-verbal sound, unclear audio),
  still provide the best possible annotation for all fields and lower the confidence score
  accordingly — never omit a segment or a required field.

Note on output validity:
The output is constrained by a JSON schema (field types, value ranges, and allowed enum values
are enforced automatically). You do not need to double-check basic type/range/enum compliance —
focus your attention on the semantic self-check below instead.

SELF-VERIFICATION (mandatory, before returning the final output):
Before returning your final answer, re-check EVERY segment for the following semantic
consistency points only (structural validity such as types, numeric ranges, and enum
membership is already guaranteed by the output schema, so do not spend effort re-checking those):

- Copied-from-input fields ("speaker", "start", "end", "multi_spk", "transcript") match the
  input JSON exactly — character-for-character for strings, value-for-value for numbers/booleans.
  Fix any dropped punctuation, altered spacing, or rounded numbers.
- categorical_emotion.primary_emotion.label does NOT also appear as a "label" value inside
  categorical_emotion.all_emotions — "all_emotions" must list only additional emotions distinct
  from the primary emotion. If you find the primary emotion duplicated inside "all_emotions",
  remove that duplicate entry before finalizing the output.
- categorical_emotion.all_emotions may be an empty list when no additional emotion beyond the
  primary is genuinely present; where more than one additional emotion is genuinely present,
  it lists multiple items rather than omitting them.
- Every other field required by the schema is present for every segment, and no extra,
  undefined fields have been added.
- The output array length equals the input segment count, and segment order matches the
  input order exactly.

Perform this verification independently for every segment — passing on one segment does
not imply another segment is compliant. Only after this verification passes for all segments
should you output the final JSON array.